import { createContext } from 'react'

export const UserStateContext = createContext('UserState')
export const AlertStateContext = createContext('AlertState')
export const ConfirmStateContext = createContext('ConfirmState')
